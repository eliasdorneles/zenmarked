[project]
name = "zenmarked"
version = "0.1.0"
description = "Standalone markdown editor with live preview and image support"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.0",
    "pillow>=10.0.0",
]

[project.scripts]
zenmarked = "zenmarked:main"
